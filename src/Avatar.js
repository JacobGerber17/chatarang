import React from 'react'

const Avatar = (props) => {
    return (
        <div classList="Avatar" style={styles}>
        
        </div>
    )
}

export default Avatar

const styles = {
    height: '40px',
    width: '40px',
    fontSize: '1rem',
    borderRadius: '20px',
}